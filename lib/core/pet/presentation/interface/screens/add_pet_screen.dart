import 'package:flutter/material.dart';

class AppPetScreen extends StatelessWidget {
  const AppPetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
